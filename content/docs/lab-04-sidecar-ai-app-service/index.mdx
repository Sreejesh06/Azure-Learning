---
title: Lab 04 - App Service Sidecars for AI
description: Master deploying multi-container Linux applications with AI inference sidecars on Azure App Service.
---

## Overview

In this lab, you will learn how to design, configure, and deploy **sidecar-enabled AI applications** on **Azure App Service**. 

An Azure App Service **sidecar** is an auxiliary container running in the exact same Linux virtual machine instance alongside your main application container. This architectural pattern allows you to run local AI model inference engines (such as Ollama, Text Embeddings Inference, or Small Language Models), telemetry collectors, or caching layers without bloating your main application image.

---

## Architectural Pattern: Main vs. Sidecar Containers

<Mermaid chart={`
%%{init: {'theme': 'default'}}%%
flowchart TB
    subgraph Client ["External Client Boundary"]
        User["Public Client<br/>(Flask App / Browser)"]
    end

    subgraph AppService ["Azure App Service Instance (Linux VM)"]
        subgraph NetNS ["Shared Network Namespace (127.0.0.1)"]
            MainContainer["Main API Container (isMain: true)<br/>(Flask - api/main.py)<br/>Port: 8080"]
            SidecarContainer["Model Server Sidecar (isMain: false)<br/>(FastAPI - model-server/main.py)<br/>ONNX Runtime GenAI (Phi-3 INT4)<br/>Port: 11434"]
        end

        SharedVolume[("Shared Disk Volume<br/>/home/models/manifest.json")]
    end

    User -->|"HTTPS Ingress (Port 8080)"| MainContainer
    MainContainer -->|"HTTP POST /generate<br/>http://localhost:11434"| SidecarContainer
    SidecarContainer -->|"Writes Manifest on Boot"| SharedVolume
    MainContainer -->|"Reads Manifest (/model-info)"| SharedVolume
`} />


## Lab Modules

In this lab module, you will build and deploy a real-world pattern:
1. **Model Server Sidecar:** A FastAPI app running ONNX Runtime GenAI that serves the **Phi-3 Mini** model.
2. **Main Application:** A Flask web application that acts as the user interface and proxies inference requests to the sidecar over `localhost`.

### Topics Covered
- [01 - Choose the App Service Sidecar Pattern](/docs/lab-04-sidecar-ai-app-service/01-choose-sidecar-pattern)
- [02 - Code and Containerize the Multi-Container App](/docs/lab-04-sidecar-ai-app-service/02-code-and-containerize)
- [03 - Configure and Deploy via ARM / Bicep](/docs/lab-04-sidecar-ai-app-service/03-configure-and-deploy)
- [04 - Diagnose Sidecar Failures](/docs/lab-04-sidecar-ai-app-service/04-diagnose-sidecar-failures)
